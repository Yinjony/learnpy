-r ../../requirements.txt

# Extra dependencies imported by the vendored Wan2.1 camera world-model path.
huggingface_hub[cli]>=0.25
lmdb>=1.4
regex>=2024.0
scipy>=1.10

# Wan attention requires FlashAttention at runtime. Install it after the
# CUDA PyTorch wheel so it builds against the active environment:
#   pip install flash-attn --no-build-isolation
